

<% if signed_in? %>
  <header class="navbar navbar-fixed-top navbar-origin navbar-rep">
<% else %>
  <header class="navbar navbar-fixed-top navbar-origin">
<% end %>

  <div class="container-fluid">

     <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#nav-menu-4">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        
    <span class="label label-warning">Beta</span>
    
    <% if signed_in? %>
          <%= link_to "Thlean : User portal", root_path, class: "navbar-brand" %>
    <% else %>
          <%= link_to "Thlean : 会計理論科目学習ポータル", root_path, class: "navbar-brand" %>
    <% end %>

      </div>

      <div class="collapse navbar-collapse" id="nav-menu-4">
        <!-- 各ナビゲーションメニュー -->
        <ul class="nav navbar-nav navbar-right">

      <li><%= link_to "ホーム", root_path %></li>
      <li><%= link_to "ヘルプ", help_path %></li>

      <% if signed_in? %>
        <li><%= link_to "メンバー", users_path %></li>
        <li id="fat-menu" class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="アカウント"> Account <b class="caret"></b>
          </a>
          <ul class="dropdown-menu">
            <li><%= link_to "プロファイル", current_user %></li>
            <li><%= link_to "ユーザ設定", edit_user_path(current_user) %></li>
            <li class="divider"></li>
            <% if current_user.admin? %>
              <li><%= link_to "Add user", new_user_path %></li>
              <li class="divider"></li>
            <% end %>
            <li>
              <%= link_to "ログアウト", signout_path, method:"delete" %>
            </li>
          </ul>
        </li>
      <% else %>
        <li><%= link_to "ログイン", signin_path %></li>
      <% end %>

        </ul>
      </div>

  </div>
</header>
